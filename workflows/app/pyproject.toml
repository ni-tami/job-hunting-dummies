[project]
name = "app"
version = "0.1.0"
description = "Poetry config for python workflows"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "grpcio>=1.78.0",
    "grpcio-tools>=1.78.0",
    "temporalio>=1.23.0",
]
