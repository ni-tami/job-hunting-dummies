# Descrption
Temporal Workflows and Tasks for Populating Database.

# Activities and Workflows
* PopulateUserWorkflow: FetchRandomUserActivity + CreateUserRPCActivity
* PopulateCompanyWorkflow: FetchCompanyActivity + RandomizeCompanyActivity + (TODO) ApproveRandomizeActivity + CreateCompanyActivity
* PopulateJobWorkflow: GetCompanyRPCActivity + GenerateJobDetailActivity + CreateJobRPCActivity
* PopulateMarketWorkflow: GetJobRPCActivity + GetUserActivity + GenerateApplicationDetailActivity + CreateApplicationActivity

---
This should be a separate repo isnt it :(
